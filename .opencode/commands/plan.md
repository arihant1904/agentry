---
description: Produce an implementation plan for a non-trivial change before any code is written.
---

Invoke the `planner` agent to produce an implementation plan for: $ARGUMENTS

If $ARGUMENTS is empty, ask what change should be planned — name the feature, refactor, or bug fix and any constraints — before delegating to the agent.
