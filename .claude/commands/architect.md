---
description: Make system and module design decisions for a feature, via the architect agent.
argument-hint: <feature or system to design>
---

Invoke the `architect` agent to design the structure of: $ARGUMENTS

If $ARGUMENTS is empty, ask what should be designed — name the feature or system and any known constraints — before delegating to the agent.
